specVersion: "v1"
serviceId: "connectverse"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "connectverse-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
